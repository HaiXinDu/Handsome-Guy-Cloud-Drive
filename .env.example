# 服务器端口
PORT=5500

# 上传目录（相对于 server.js 的路径）
UPLOAD_DIR=uploads

# 上传限制（已移除大小和数量限制）

# 禁止上传的文件扩展名（逗号分隔）
BLOCKED_EXTENSIONS=.exe,.bat,.cmd,.scr,.pif,.com,.sh,.ps1

# 功能开关 (true/false)
ENABLE_LOGGING=true
ENABLE_SANITIZATION=true
CACHE_FOLDER_SIZE=true

# 文件夹大小缓存 TTL（毫秒）
CACHE_TTL=60000
